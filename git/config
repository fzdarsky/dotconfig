[user]
name = Frank A. Zdarsky
email = fzdarsky@redhat.com
signingkey = $HOME/.ssh/id_fzdarsky_redhat_gh.pub

[gitreview]
username = fzdarsky

[credential]
helper = cache

[gpg]
format = ssh

[gpg "ssh"]
allowedSignersFile = $HOME/.config/git/allowed_signers

[pull]
ff = only

[color]
ui = auto
