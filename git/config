[user]
name = Frank A. Zdarsky
email = fzdarsky@redhat.com
signingkey = ~/.ssh/id_fzdarsky_redhat_gh.pub

[gitreview]
username = fzdarsky

[credential]
helper = cache

[gpg]
format = ssh

[gpg "ssh"]
allowedSignersFile = ~/.config/git/allowed_signers

[init]
defaultBranch = main

[pull]
ff = only

[color]
ui = auto
